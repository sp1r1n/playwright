## What changed

<!-- One or two sentences. New tests, a fixed flake, a framework change? -->

## Why

<!-- The problem being solved. Link an issue if there is one. -->

## Verification

- [ ] `npm run check` passes (types, lint, formatting)
- [ ] `npm test` passes locally
- [ ] New tests ran more than once, so a flake would have shown up
- [ ] Allure metadata (feature, story, severity) set on any new test
